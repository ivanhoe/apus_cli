# Nonstandard Layout Fixture

This fixture intentionally hides the Xcode project below the supported search depth.

`apus init` should fail cleanly from the fixture root and leave all files untouched.
